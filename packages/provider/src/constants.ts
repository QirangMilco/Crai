/** provider 包内私有常量。不属于 core 跨层共享契约。 */

/** 通用 HTTP 常量 */
export const HTTP = {
  METHOD_POST: 'POST',
  CONTENT_TYPE_JSON: 'application/json',
} as const

/** 消息 ID 前缀 */
export const ID_PREFIX = 'msg_'
